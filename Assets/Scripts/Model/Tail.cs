namespace DefaultNamespace
{
    public class Tail
    {
        public Vector2 Position;
        public Vector2 NextPosition;

        public Tail(Vector2 position, Vector2 nextPosition)
        {
            Position = position;
            NextPosition = nextPosition;
        }
    }
}